# Configure npm to handle optional dependencies better
optional=false
# Disable platform-specific binaries that cause issues on Vercel
platform=linux
arch=x64
# Use npm's legacy peer deps to avoid conflicts
legacy-peer-deps=true 